<!-- HEADER (STICKY) -->
<div class="sticky top-0 z-30 bg-white/90 backdrop-blur border-b border-gray-200 px-2 md:px-4 py-2 mb-4">
    <div class="flex items-center justify-between">

        <!-- LOGO -->
        <img src="{{ asset('images/logo.png') }}" class="w-24 md:w-28" alt="Koma Gardens Logo">

        <!-- ACTIONS -->
        <div id="top-nav" class="flex items-center gap-4">

            <!-- ADVANCED SEARCH -->
            <a href="#" id="advanced-search-link" title="Advanced Search" class="flex items-center gap-1 text-indigo-600 hover:text-indigo-800 whitespace-nowrap">
                <i class="fa-solid fa-magnifying-glass-plus"></i>
                <span class="text-xs">Advanced</span>
            </a>


            <!-- STK PUSH -->
            <button id="show-stk" title="STK Push" class="flex items-center gap-1 text-purple-600 hover:text-purple-800">
                <i class="fa-solid fa-mobile-screen-button"></i>
                <span class="text-xs">STK</span>
            </button>

            <!-- REFRESH -->
            <button id="refresh" title="Refresh" class="flex items-center gap-1 text-blue-600 hover:text-blue-800">
                <i class="fa-solid fa-rotate"></i>
                <span class="text-xs">Refresh</span>
            </button>

            <!-- LOCK -->
            <button id="lock" title="Lock" class="flex items-center gap-1 text-yellow-600 hover:text-yellow-700">
                <i class="fa-solid fa-lock"></i>
                <span class="text-xs">Lock</span>
            </button>

            <!-- LOADER -->
            <span class="loader text-sm text-gray-500 ml-1">Loading...</span>
        </div>
    </div>
</div>
